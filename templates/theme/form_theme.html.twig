{% use 'bootstrap_5_layout.html.twig' %}

{#
    Utilisation des sélecteurs "type de champ"_"fonction de rendu"
    Permet d'altérer un rendu ciblé
 #}
{% block integer_label %}
    <p>Saisissez le tarif en centimes</p>
    {{ block('form_label') }}
{% endblock %}

{% block form_label_content %}
    {{ parent() }}
    {% if required %}<span class="badge text-bg-warning">requis</span> {% endif %}
{% endblock %}
